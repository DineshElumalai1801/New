pipeline {
    agent any
    stages {
        stage ('Maven Checks')
        {
            steps {
                sh 'mvn --version'
            }
        }
         stage ('Maven Checks')
        {
            steps {
                sh 'docker --version'
            }
        }
}
}
